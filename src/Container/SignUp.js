import React from "react";
import SignUpUser from "../components/AuthComponents/SignUp";

// These are the main route pages that has been used in routing in the config folder

const SignUp = () => {
  return (
    <div>
      <SignUpUser />
    </div>
  );
};

export default SignUp;
