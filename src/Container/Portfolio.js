import React from "react";
import PortfolioComponent from "../components/PortfolioComponents/PortfolioComponent";

// These are the main route pages that has been used in routing in the config folder

const Home = () => {
  return (
    <div>
      <PortfolioComponent />
    </div>
  );
};

export default Home;
